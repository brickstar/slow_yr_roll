// Place all the styles related to the songs controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/
audio:hover, audio:focus, audio:active
{
-webkit-box-shadow: 15px 15px 20px rgba(0,0, 0, 0.4);
-moz-box-shadow: 15px 15px 20px rgba(0,0, 0, 0.4);
box-shadow: 15px 15px 20px rgba(0,0, 0, 0.4);
-webkit-transform: scale(1.05);
-moz-transform: scale(1.05);
transform: scale(1.05);
}

audio
{
-webkit-transition:all 0.5s linear;
-moz-transition:all 0.5s linear;
-o-transition:all 0.5s linear;
transition:all 0.5s linear;
-moz-box-shadow: 2px 2px 4px 0px #006773;
-webkit-box-shadow:  2px 2px 4px 0px #006773;
box-shadow: 2px 2px 4px 0px #006773;
-moz-border-radius:7px 7px 7px 7px ;
-webkit-border-radius:7px 7px 7px 7px ;
border-radius:7px 7px 7px 7px ;
}

.navbar {
  color: red;
  height: 5em;
  margin-bottom: -20px !important;
}

.navbar-brand {
  color: #17457F;
  font-size: 2em;
  font-family: 'Monoton', cursive;
  position: absolute;
  right: .5em;
}

.logo {
  position: absolute;
  padding-top: .2em;
  font-size: 2.7em;
  text-align: center;
}

.fa-itunes-note {
  color: pink;
  margin-left: .4em;
}

.song-page-box {
  padding-right: 4em;
  background-image: url(<%= asset_path("rainbow-sinky.jpg")%>);
  height: 66em;
  opacity: .9;
}

.song-page-content-box{
  padding-top: 4em
}

.audio-player-box{
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 4em;
}

#rate-number {
  color: #1C457F;
  font-size: 2em;
  font-family: 'VT323', monospace;
}

.song-list {
  color: #1C457F;
  font-size: 5em;
  font-family: 'VT323', monospace;
}

.song_audio {
  font-family: 'Concert One';
}

#song-playlist {
  margin-bottom: .4em;
}

.upload-box {
  display: flex;
  align-items: center;
  flex-direction: column;
}

.playlist-box {
  margin-left: 9em;
  display: flex;
  flex-direction: column;
}

.form-box {
  font-family: 'Concert One';
  color: #6AFFBE;
  font-size: 2em;
  margin-top: .65em;
  width: 2em;
  margin-right: 10em;
}

.playlist-form-box {
  padding-left: 8em;
  display: flex;
  flex-direction: row;
  justify-content: center;
}

.list-group-item {
  display: flex;
  justify-content: center;
  align-items: center;
}

.song-title {
  border-radius: .2em;
  color: #6AFFBE;
  font-size: 2.5em;
  font-family: 'Concert One', light;
}

.fa-trash-alt {
  color: #FF9748;
  font-size: 2em;
}

#song-index {
  color: #6AFFF7;
  margin-right: .3em;
  margin-top: .5em;
}

#playback-rate-form-box {
  display: flex;
  align-items: center;
  justify-content: center;
}

#playback-rate-form {
  width: 20em;
  margin-top: 2em
}

#song_audio {
  font-size: .6em;
  border-radius: .2em;
}

#myAudio {
  border-radius: 2em;
}

#speed-alert {
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
  margin-right: 1em;
  border-radius: .6em
}

#set-speed {
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
  margin-left: 1em;
  border-radius: .6em
}

#footer-text {
  top: 1em;
  position: absolute;
  font-size: .6em;
  font-family: roboto, cursive;
  right: 1em;
}

footer {
  background-color: #E5FFFD;
  position: fixed;
  height: 2em;
  bottom: 0;
  width: 100%;
}
